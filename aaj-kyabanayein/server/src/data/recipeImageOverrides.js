/** Curated thumb overrides — empty until recipes are added manually. */
export const DIRECT_THUMB_OVERRIDES = {};

export function getDirectThumbOverride() {
  return null;
}

export function getImageSearchOverride() {
  return null;
}
